# InkScry TODO

## P2 — 定时同步（已实现 2026-08-23）

- 背景：事件驱动的盲区是「不用 Claude 的时候」——额度重置/余额变化不会上屏
  （实例：Codex 周额度 08-21 重置，屏上挂着 0% 直到 08-23 手动推送）。
- 架构：高频查（电脑侧 HTTP 免费）低频刷（设备侧全刷昂贵）。
  `--sync` 单轮：查额度 → 屏显精度签名比对（`last_pushed_state.json`）→
  有变化才推；`--watch` 常驻循环；`inkscry-bar` macOS 菜单栏程序（rumps，
  可选依赖，菜单内看面板数据/立即刷新/暂停）。
- 配置：`INKSCRY_SYNC_INTERVAL`（默认 900s）/ `INKSCRY_QUIET`（静默时段）/
  `INKSCRY_HEARTBEAT`（超时强制推，默认关）。hook 推送优先，sync 自动让路；
  底栏状态无 hook 时按会话日志 mtime 推断。
- 比对同样作用于 hook 路径（2026-08-23）：普通事件过防抖后数据没变也跳过；
  签名含顶部告警横幅（waiting/error 必须参与，防过期告警滞留）；镜像文件
  跟随每一次成功推送（含 --demo），清屏（CLI 与菜单栏）作废镜像。

## P3 — 刷新防抖

- 背景：墨水屏全刷是耗电大头（毫安级 × 十几秒），频繁 hook 事件（如连续 Stop）会反复全刷，耗电且伤屏。
- **已实现（2026-08-19）**：`INKSCRY_PUSH_INTERVAL`（默认 60s）内普通事件跳过推送；
  等待确认/出错类事件必推。时间戳记在 `~/.cache/inkscry/last_push`。

## P3 — 局部刷新（路线 A：局部写 RAM + 全刷波形）

- 背景：全屏推送 30KB（双平面）约 9s，但多数事件只有状态栏/时间变化。
- 依据：固件 `0x03 SEND_CMD` / `0x04 SEND_DATA` 是 SPI 透传后门，可在电脑端自拼时序
  `PTIN → SetWindow(x,y,w,h) → DTM1/DTM2 → PTOUT → 0x05`，无需改固件。
- **实测结论（2026-08-19，右下角时钟区 64×28 窗口）：链路完全走通，但工程价值不足，放弃**。
  实验一（跨连接）：局部写成功（448B/0.38s，窗口内正确更新），窗口外全屏雪花——
  固件 `on_disconnect` 断面板电，RAM 不存活。
  实验二（同一连接内 全推→局部写）：窗口外干净、更新正确，证明 RAM 在连接存续
  期间可靠，「常驻连接 + 局部更新」可行。
  **但**：局部更新只省 BLE 传输（9s→0.5s），屏幕闪烁来自 OTP 全刷波形本身，
  与传输量无关；三色屏无局刷波形（路线 B 已排除），任何内容变化必然全屏闪。
  对事件驱动场景，传输不是痛点、闪屏才是，而闪屏软件无解——故不做常驻连接方案。
- P4 若刷固件可顺带解锁：on_disconnect 保持面板供电（RAM 存活）+ 固件内局部
  刷新指令，但也只能省传输，闪屏依旧。
- 真局刷（路线 B，不闪屏）已核实基本不可行：微雪 4.2" 黑白版（EPD_4in2_V2）局刷靠
  OTP 局刷波形（0x22 0xFF 序列；233B 自定义 LUT_ALL 实为 4 灰阶用），而三色版
  （EPD_4in2b_V2，同为 UC8176）官方驱动无任何 partial/LUT 支持——红色粒子响应慢，
  短波形驱动不动。本机 model_id=0x03 即 BWR 变体。

## P4 — 固件改造（字库模式 + 电压回传）

- 字库模式：固件内置 8x16 点阵字库，电脑端只发几十字节 ASCII，传输 5s → 0.1s。
- 电压回传：加一条 `0x07` 指令调 `EPD_ReadVoltage()` 经 notify 回 `v=3012`；
  固件已有 SAADC 测量，只是没接 BLE。电脑端换算百分比显示。
- 条件：设备支持 FE59 Buttonless DFU（OTA 刷机）；硬件引脚与上游 EPD_CFG_52811 一致。
  风险：设备固件为 20 字节配置的 fork 版本（上游 13 字节），刷纯上游可能丢失 fork 功能。
  建议两件事攒到一起刷一次机。


## P4 — 物理按键回传

- 若外壳有按键 GPIO：bleak 订阅 notify，按键触发电脑端操作（git commit / 给 Claude 回 y）。
